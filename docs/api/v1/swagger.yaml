info:
  contact: {}
paths:
  /tasks:
    get:
      description: list tasks
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: list example
      tags:
      - example
swagger: "2.0"
